# Purpose: ScanNet++ download helper (tokenized access after approval)
print("ScanNet++ requires account + application approval + personalized token.")
print("After download, place under: data/scannetpp/<scene_id>/...")
