# Purpose: Baseline TSDF config (matched to ours TSDF resolution)
data:
  root: data/synth/scene0000
  stride: 1
  max_frames: 60
  depth_scale: 1000.0
tsdf:
  voxel: 0.05
  trunc: 0.25
output:
  out_dir: outputs/base_v005
